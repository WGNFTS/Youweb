// Simulação de recomendações
function getVideoRecommendations(userId) {
    // Recomendações com base no histórico de visualização (simulado)
    if (userId === 1) {
        return ['video1', 'video2', 'video3'];
    }
    return [];
}

function updateRecommendations(userId, videoId) {
    // Atualize as recomendações com base na visualização do usuário (simulado)
}
